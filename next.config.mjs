/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "fresh-spirit-281e92512c.media.strapiapp.com",
      "fresh-spirit-281e92512c.strapiapp.comhttps",
    ],
  },
};

export default nextConfig;
